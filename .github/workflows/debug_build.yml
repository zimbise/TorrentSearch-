name: Build Debug APK

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      """DEPRECATED: This workflow was removed. Use `android-ci.yml` which builds a non-debuggable APK."""
  pull_request:
